---
# Ansible Role Metadata
# For complete information about all available options:
# https://galaxy.ansible.com/docs/contributing/creating_role.html#role-metadata

dependencies:
  - role: geerlingguy.git
    become: true
  - role: geerlingguy.security
    vars:
      security_ssh_allowed_users: "{{ common_ssh_users }}"
    become: true

galaxy_info:
  role_name: common
  author: Victor Seva
  description: common role for kamailio-infra
  license: GPLv3

  min_ansible_version: "2.11"
  galaxy_tags: []
  platforms:
    - name: Debian
      versions:
        - all
